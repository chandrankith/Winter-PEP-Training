Day-6 of our PEP training:

we solved a question on string manipulation.

Then started with "Inheritance" with an example of Car as a class and inherting car models as a child.

Then had some basic of "Methods and variables of classes".

Then acknowledged basic OOPS of access specifiers (Public,Protected,Private) .

